%body.user_show
  = render 'shared/top_menu_bar'

  = render 'shared/side_menu_bar'

  %div#fabric_input_field
    %p Enter the Fabric S/N:
    / Create a form to input the serial number
  
  %div#fabric_list
    / List out all the fabric just name and price
    %table.table.table-hover
      %thead
        %tr
          %th Color
          %th Serial
          %th Price
      %tbody
        - if @fabrics.any?
          - @fabrics.each do |fabric|
            %tr
              %td= link_to "#{fabric.color}", user_fabric_path(current_user, fabric)
              %td= link_to "#{fabric.serial}", user_fabric_path(current_user, fabric)
              %td= fabric.price

